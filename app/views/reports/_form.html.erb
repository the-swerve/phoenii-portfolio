<%= form_for [@business,@report] do |f| %>
  <%= render :partial => 'layouts/form_errors', :locals => {:object => @report} %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
